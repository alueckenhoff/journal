import Petal from "./Petal";

export default function Petals( ) {

    //TODO logic for randomX: random pixels from top petal starts out

    //TODO logic for randomY: random % for how left before turns

    return <div>
        <Petal version={ 1 } randomX={40} randomY={20} />
    </div>
}